{
  "name": "claude-mcp-lp-example",
  "version": "1.0.0",
  "description": "Claude Code + Playwright MCP + Figma MCP を使用したLP制作例",
  "main": "index.js",
  "scripts": {
    "start": "live-server --port=3000 --open=/index.html",
    "dev": "live-server --port=3000 --open=/index.html",
    "build": "echo 'Building static site...' && mkdir -p dist && cp -r *.html *.css *.js images dist/",
    "deploy": "vercel --prod"
  },
  "keywords": [
    "claude-code",
    "mcp",
    "playwright",
    "figma",
    "landing-page"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "tailwindcss": "^3.3.0"
  }
}